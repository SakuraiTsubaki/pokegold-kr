# Asset Workflow

Assets should remain traceable from Korean source evidence to reviewable and reproducible project output.

1. Record target version/revision and source location.
2. Preserve original identifiers, indices, ordering, dimensions, palettes, text/font relationships, and containers when known.
3. Keep editable or source-faithful representations alongside generated forms when useful.
4. Deduplicate only after byte identity or equally strong reproducible equivalence is confirmed.
5. Preserve genuinely different variants.
6. PNG previews are encouraged for human review when provenance is recorded.
7. Generated assets should identify tool, method, inputs, and hashes.

Retail ROM images are never project artifacts and must not be committed.
